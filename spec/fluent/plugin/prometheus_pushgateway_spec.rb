RSpec.describe Fluent::Plugin::PrometheusPushgateway do
  it "has a version number" do
    expect(Fluent::Plugin::PrometheusPushgateway::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
